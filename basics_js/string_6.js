const name='shreejoy'
const count=40

console.log(name+count+'value');
//mordern sting input 
console.log(`hello my name is ${name} and my count is ${count}`);

const gameName=new String("IGI") // another way of defining the string variable
console.log(gameName[0])